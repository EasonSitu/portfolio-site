# 个人网站视觉改版提示词

你正在接手一个已经可以正常运行的三语个人求职网站。请先完整阅读项目根目录的 `HANDOFF.md`，再检查现有页面、代码和预览截图。

项目位置：

```text
D:\codex\Eason\portfolio-site
```

## 你的任务

当前版本在功能、内容结构、三语切换和主要动效方面已经达到 MVP 要求，但整体审美仍然像常见的黑色加紫色光效开发者作品集，缺乏鲜明、成熟且与候选人定位一致的视觉气质。

请在保留网站核心内容和功能的前提下，重新建立一套更完整的视觉设计语言，并直接实现一个可以在浏览器中查看的版本。

这次工作的重点是：

- 视觉方向
- 页面构图
- 字体和信息层级
- 色彩与材质
- 区块节奏
- 动效体系
- 英文、简体中文和繁体中文的排版质量

这次工作的重点不是：

- 重新改写简历
- 增加虚构项目
- 把候选人包装成资深 AI 工程师或底层 IoT 工程师
- 为了炫技加入影响阅读和性能的复杂效果

## 网站定位

网站主要给香港招聘人员和岗位负责人查看，是简历之后的第二层证明材料。

候选人的核心定位是：

> 能够连接业务需求、技术团队与实际交付，把模糊问题整理成明确需求、可执行方案、测试流程和最终成果的人。

网站应当让招聘者在 30–60 秒内理解候选人的方向、能力和经历，并产生进一步邀请面试的兴趣。

## 开始前必须完成

1. 阅读 `HANDOFF.md`。
2. 打开当前本地网站，分别检查桌面端和手机端。
3. 查看以下预览图：
   - `output/preview/hero-final.png`
   - `output/preview/mobile-zh-hk.png`
   - `output/preview/experience.png`
4. 阅读：
   - `components/Portfolio/Portfolio.js`
   - `components/Portfolio/Portfolio.module.scss`
   - `data/content.mjs`
   - `utils/i18n.mjs`
5. 确认理解内容边界、三语结构和目前的代码组织方式。

## 设计要求

请选定一个清晰、统一的视觉方向，不要把多个互相冲突的风格随意拼在一起。

可以优先探索以下方向之一，也可以提出更好的方向：

### 方向一：Editorial Systems

成熟、精确、有编辑设计感。通过强字体层级、网格、编号、线条、留白和克制的色彩建立专业度。

### 方向二：Spatial Signal Objects

将业务、技术和交付转化为有空间感的模块、信号、路径或动态对象。可以使用 GSAP、SVG、CSS 3D 或轻量 Canvas，但内容必须始终清楚可读。

### 方向三：Hong Kong Operational Systems

从香港城市系统、交通导视、技术标识、玻璃与金属材质、密集但有秩序的信息结构中提取设计语言，但避免工地黄黑条、蓝图背景等廉价工业风格。

无论选择哪个方向，都需要：

- 让首屏在五秒内说明“是谁、做什么、下一步看哪里”。
- 改善中文字体和中英文混排，不能让中文版像英文模板的附属翻译。
- 让不同区块拥有清楚的视觉节奏，不要全站都是相同的暗色描边卡片。
- 让工作经历成为最有说服力的内容区，而不是只有动效、没有信息重点。
- 保留一定的 Devfolio 动态感，但重新定义动效的作用。
- 动效必须服务于进入、定位、转换和反馈，不要所有元素都使用同一种向上淡入。
- 移动端必须是重新组织后的布局，而不是简单缩小桌面端。
- 保留 reduced-motion 可用性。

## 内容和事实边界

- 保留英文、简体中文和繁体中文三个版本。
- 所有内容继续集中放在 `data/content.mjs`，不要把翻译散落在 JSX 中。
- 不得修改公司名称、任职日期、项目数字和职责边界。
- 不得添加未经确认的 LinkedIn 地址。
- 不得虚构 Selected Work 或项目案例；目前项目区暂时不做。
- IoT 经验应围绕多设备方案部署统筹、现场实施、测试、调试、验收和问题处理，不要写成底层集成研发。
- AI 经验属于应用与原型实践，包括知识库、工作流自动化、封装的 Python 工具、网页原型和 YOLO/OpenCV/MediaPipe Demo，不要包装成模型训练或算法研究。
- 保留简历下载、邮箱联系和原 Devfolio MIT attribution。

## 执行方式

请不要一开始就向我提出大量问题，也不要重新进行完整需求访谈。

按照下面的步骤执行：

1. 用不超过 300 字总结当前页面最主要的审美问题。
2. 明确说明你选择的视觉方向，并列出：
   - 色彩方案
   - 字体方案
   - 网格与留白
   - 首屏构图
   - 工作经历区构图
   - 动效原则
3. 然后直接开始实现，不需要等待我再次确认。
4. 优先修改视觉系统和核心页面，不要重构与改版无关的代码。
5. 完成后运行测试和生产构建。
6. 输出桌面端和手机端截图。
7. 简要列出改版前后的关键差异，以及仍值得继续优化的部分。

## 技术限制

- 继续使用当前 Next.js 14 Pages Router 项目。
- 可以继续使用 GSAP / ScrollTrigger。
- 可以使用项目中已经安装的 Framer Motion，但不要为了使用依赖而使用。
- 优先使用现有技术栈完成，不要引入后端、数据库、CMS 或登录系统。
- 如果考虑 WebGL，必须先判断它是否真正提升职业表达；如果只是背景装饰，请使用更轻量的 SVG、CSS 或 Canvas。
- 不要删除 `LICENSE.md` 或原作者 attribution。

## 验收要求

完成后至少验证：

- `pnpm test`
- `pnpm build`
- 1440 × 1000 桌面端
- 1024 × 768 中等屏幕
- 390 × 844 手机端
- 英文、简体中文、繁体中文分别检查
- 页面无横向溢出、文字遮挡和关键内容裁切
- 无动画时仍然能够完整阅读

最终交付必须包括：

1. 已实现的代码。
2. 桌面和手机预览图。
3. 设计方向说明。
4. 主要改动列表。
5. 测试和构建结果。

请现在开始：先检查当前实现，说明选定方向，然后直接完成视觉改版。

